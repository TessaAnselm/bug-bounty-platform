version: v1.25.0
# No ignores. The open-redirect findings these once suppressed are now fixed in
# code: redirect id segments are validated with uuid.UUID() and the auto-login
# middleware redirects to a constant path. `snyk code test` reports 0 issues.
